# Copy to .env.local (gitignored) and fill in real values.
# Same Supabase project as the Android app — same accounts, same data, same RLS.
# The anon key is safe to ship in a client bundle; RLS is what protects the data.

VITE_SUPABASE_URL=https://ybciyuylaofvupdqqucu.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
