# Minification is disabled in release for now (see app/build.gradle.kts). When you
# enable it, supabase-kt + ktor + kotlinx.serialization need keep rules. Start from
# the rules in the supabase-kt README / TROUBLESHOOTING and test a release build
# thoroughly before distributing.
